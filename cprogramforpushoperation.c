#include<stdio.h>
int main()
{
int top,stack[top],n,data;
if(top==n)
{
printf("\n overflow");
}
else
{
     printf("\n enter the element");
     scanf("%d",&n);

     top=top+1;

     stack[top]=data;

     return 0;
}
}
